# 应用配置
app:
  name: "hkchat-nutshell-api"
  version: "1.0.0"
  env: "dev"
  port: 18081
  debug: true
  swagger:
    host: "/"
    scheme: "http"
    base_path: "/api/v1"

# 数据库配置 - PostgreSQL
database:
  driver: "postgres"
  host: "nas.helwd.com"
  port: 15432
  user: "postgres"
  password: "!Qq868285"
  dbname: "hkchat"
  sslmode: "disable"
  timezone: "Asia/Shanghai"
  max_open_conns: 100
  max_idle_conns: 10
  log_level: "info"

# JWT配置
jwt:
  secret_key: "your-secret-key-here"
